# This is a workflow file based on the current template from the repository link shared by the user.

permissions:
  actions: read
  contents: read
  security-events: write

jobs:  # Retaining the "jobs:" block without changes.